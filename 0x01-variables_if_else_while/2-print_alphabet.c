#include <stdlib.h>

#include <time.h>
#include <stdio.h>

/**
 * main - a function that print the last digit of an integer
 * ------
 *  Return: zero
 */
int main(void)

{
	char alpha = 'a';

	while (alpha <= 'z')
	{
		putchar(alpha);
		alpha++;
	}
	return (0);

}
