#include "main.h"
#include <stdio.h>
#include <string.h>
#include <ctype.h>
/**
 * _islower - checs whether the argurment is lower case
 * @c: - an argurment to be checked
 *  Return: 1 if true , 0 if fasle
 */

int _islower(int c)
{
	if (islower(c) > 0)
		return (1);
	else
		return (0)
}

