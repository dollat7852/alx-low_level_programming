#include "main.h"
#include <stdio.h>
#include <string.h>
#include <ctype.h>
/**
 * _isalpha - checs whether the argurment is lower case
 * @c: - an argurment to be checked
 *  Return: 1 if true , 0 if fasle
 */

int _isalpha(int c)
{
	if (isalpha(c) == 0)
		return (0);
	else
		return (1)
}

